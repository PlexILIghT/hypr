# exec = export SLURP_ARGS='-d -c 061F25BB -b CEE7EF44 -s 00000000'

general {
    col.active_border = rgba(171C1E39)
    col.inactive_border = rgba(6D767930)
}

misc {
    background_color = rgba(F5FAFCFF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(F5FAFCFF)
        col.text = rgba(171C1EFF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(171C1E), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(171C1E), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(171C1E), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(00687BAA) rgba(00687B77),pinned:1